\documentclass[12pt]{article}

%----------------------------------------------------------------------------------------
% MAIN CONFIGURATION

\usepackage{graphicx}
\usepackage{nopageno} % Remove all page number
\usepackage{polyglossia} %LuaLatex Compiler Engine
\setmainlanguage[variant=indonesian]{malay}

\usepackage{geometry} % Required for adjusting page dimensions and margins

\geometry{
	paper=a4paper, % Paper size, change to letterpaper for US letter size
	top=2.5cm, % Top margin
	bottom=3cm, % Bottom margin
	left=4cm, % Left margin
	right=4cm, % Right margin
	headheight=0.75cm, % Header height
	footskip=1.5cm, % Space from the bottom margin to the baseline of the footer
	headsep=0.75cm, % Space from the top margin to the baseline of the header
	showframe, % Uncomment to show how the type block is set on the page
}
\usepackage{subfiles}
\usepackage[table]{xcolor}

\definecolor{White}{rgb}{255,255,255}
\definecolor{gray}{gray}{0.98}
\definecolor{dgray}{gray}{0.58}
\def\ccg{\cellcolor{dgray}}


\def\cca#1{%
\ifdim#1pt<15pt\cellcolor{green!90}\ifnum#1<15\color{green!90}\fi\else
\ifdim#1pt<30pt\cellcolor{yellow!90}\ifnum#1<30\color{yellow!90}\fi\else
\cellcolor{red!90}\color{red!90}\fi\fi
#1}


\usepackage{comment}
%----------------------------------------------------------------------------------------
% CONFIGURATION OF Tikz
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows,arrows.meta}
\pgfarrowsdeclarecombine{twotriang}{twotriang}%
{Rays}{}{Stealth}{Stealth}

\def\checkmark{\tikz\fill[scale=0.5](0,.35) -- (.25,0) -- (1,.7) -- (.25,.15) -- cycle;}

% Configuration of Tikz FlowChart
\tikzstyle{startstop} = [rectangle, rounded corners, minimum width=3cm, minimum height=1cm,text centered, draw=black]

\tikzstyle{arrow} = [thick,->,>=stealth]
\tikzstyle{doublearrow} = [thick,<->,>=stealth,line width=1.5pt]
\tikzstyle{dotted} = [loosely dotted,>=stealth]

%Configuration of Tikz Piechart
\usepackage{pgf-pie}

%Configuration of Tikz Barchart
\usepackage{pgfplots} % Extention of tikz for expand geometry function
\pgfplotsset{compat=newest}
%----------------------------------------------------------------------------------------
% PREAMBLE OF TABLE AND LIST

\usepackage{tabularx}
\usepackage{booktabs} % Required for better horizontal rules in tables
\usepackage{multirow}

\usepackage{dcolumn} % pretty column descr R crosstab2tex
\newcolumntype{d}{D{.}{.}{-1}} % this will add extra space for decimal numeric on the right, this will remove it D{.}{.}{0}

%change font percentage symbol
\newcommand*\pct{\mbox{%
    \fontencoding{\encodingdefault}%
    \fontfamily{cmr}%
    \selectfont
    \symbol{`\%}%
  }%
  }
\newcommand*\pc{{\scalebox{.9}{\pct}}}

\usepackage{paralist} %Inline list(inparaenum), always on top of enumitem
\usepackage{lscape}
\usepackage{longtable} % Enable table cross multiple page
\usepackage{array} %Mostly formatting columns and spacing
\usepackage{diagbox} % Diagonal line on table
\newcolumntype{P}[1]{>{\centering\arraybackslash}p{#1}} %centering elemen in table
\newcolumntype{R}[1]{>{\columncolor{gray}}p{#1}} %centering elemen in table
\usepackage{enumitem} % Required for list customisation

\setlist{noitemsep} % No spacing between list items
\setlist[itemize]{label=\textbullet, nosep, topsep=0pt, partopsep=0pt, parsep=0pt} % No spacing between itemize

\newcommand{\tabitems}{~~\llap{\textbullet}~~}

% Remove spacing between itemize env. inside table env. this corespond with longtable.tex
\makeatletter
% Enumerated columns
\newcolumntype{e}[1]{%
    >{\minipage[t]{\linewidth}\let\\\tabularnewline
      \enumerate
      \addtolength{\rightskip}{0pt plus 50pt}% for raggedright
      \setlength{\itemsep}{-\parsep}}%
    p{#1}%
    <{\@finalstrut\@arstrutbox\endenumerate\endminipage}}

% Itemized columns
\newcolumntype{i}[1]{%
    >{\minipage[t]{\linewidth}\let\\\tabularnewline
      \itemize
      \addtolength{\rightskip}{0pt plus 50pt}% for raggedright
      \setlength{\itemsep}{-\parsep}}%
    p{#1}%
    <{\@finalstrut\@arstrutbox\enditemize\endminipage}}
\makeatother

\newcommand{\tabitem}{~\llap{\textbullet}}

\begin{document}

%\subfile{subfiles/flowchart1.tex}
%\clearpage
\subfile{subfiles/flowchart2.tex}
%\clearpage
\subfile{subfiles/flowchart3.tex}
%\subfile{subfiles/flowchart4.tex}
\clearpage
%\subfile{subfiles/longtable.tex}
%\subfile{subfiles/piechart.tex}

%\subfile{subfiles/diagtab.tex}
\end{document}
